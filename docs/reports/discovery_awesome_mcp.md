# Discovery Report: awesome-mcp-servers Deep Dive

> Generated by `site-analyzer` skill.

## High-Value Assets Identified

The following MCPs and Skills were found during the crawl of `https://github.com/punkpeye/awesome-mcp-servers`.

### 1. Skill Ninja MCP (`aktsmm/skill-ninja-mcp-server`)

- **Category**: Developer Tools / Automation
- **Why it fits**: It specifically manages `SKILL.md` files (the exact format we use). It can search and install skills from GitHub directly into the workspace.
- **Action**: Propose registration in `mcp/docker_registry.yml`.

### 2. Context Awesome (`bh-rat/context-awesome`)

- **Category**: Research / Knowledge
- **Why it fits**: Provides an MCP interface to 8,500+ curated awesome lists. This would supercharge the `@researcher` agent by giving it a structured way to find any tool.
- **Action**: Propose registration.

### 3. Roundtable (`askbudi/roundtable`)

- **Category**: Orchestration
- **Why it fits**: Unifies multiple AI assistants (Claude, Cursor, Gemini). Highly relevant for the `agent-teams` philosophy of the brain repo.
- **Action**: Propose registration.

### 4. Mengram (`alibaizhanov/mengram`)

- **Category**: Memory
- **Why it fits**: Adds an episodic and procedural memory layer. Useful if we decide to extend beyond Engram/Knowledge Graphs.

## Candidate Registry Update

```yaml
  skill-ninja:
    image: aktsmm/skill-ninja-mcp-server
    tag: latest
    purpose: Automated discovery and installation of agent skills (SKILL.md)
    when_to_use: Expanding the brain's capabilities with community skills
    docker_command: |
      docker run -i --rm aktsmm/skill-ninja-mcp-server:latest
```
