# Skill Context: Next.js App

- Be explicit about server vs client components.
- Fetch data on the server by default; move to client only for interactivity.
- Keep route handlers thin and validate request input early.
- Treat caching and revalidation as part of the spec, not an afterthought.
- Avoid leaking secrets to the client bundle.
