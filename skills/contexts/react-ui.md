# Skill Context: React UI

- Keep state close to where it is used and lift it only when necessary.
- Prefer composition over prop drilling through many layers.
- Model loading, empty, error, and success states explicitly.
- Use semantic HTML and keyboard-accessible interactions by default.
- Test behavior and user flows, not component internals.
