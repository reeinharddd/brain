# Skill Context: Python Service

- Prefer `uv` or the repo's existing environment tool for reproducibility.
- Use type hints on public functions and boundary objects.
- Keep side effects at the edges and business logic in importable modules.
- Validate external data with explicit schemas or typed adapters.
- Run tests and static checks before declaring a task done.
