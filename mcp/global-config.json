{
  "mcpServers": {
    "brain-gateway": {
      "command": "curl",
      "args": [
        "-sf",
        "http://localhost:3000/health"
      ],
      "description": "MCP Gateway health check - all MCPs accessible via /mcp/{name}/sse"
    },
    "memory": {
      "command": "curl",
      "args": [
        "-sf",
        "http://localhost:3000/mcp/memory/sse"
      ],
      "description": "Persistent memory via MCP Gateway"
    },
    "filesystem": {
      "command": "curl", 
      "args": [
        "-sf",
        "http://localhost:3000/mcp/filesystem/sse"
      ],
      "description": "Filesystem operations via MCP Gateway"
    },
    "sequential-thinking": {
      "command": "curl",
      "args": [
        "-sf", 
        "http://localhost:3000/mcp/sequential-thinking/sse"
      ],
      "description": "Sequential thinking via MCP Gateway"
    },
    "context7": {
      "command": "curl",
      "args": [
        "-sf",
        "http://localhost:3000/mcp/context7/sse"
      ],
      "description": "Context7 documentation via MCP Gateway"
    },
    "github": {
      "command": "curl",
      "args": [
        "-sf",
        "http://localhost:3000/mcp/github/sse"
      ],
      "description": "GitHub operations via MCP Gateway"
    },
    "skill-ninja": {
      "command": "curl",
      "args": [
        "-sf",
        "http://localhost:3000/mcp/skill-ninja/sse"
      ],
      "description": "Skill detection via MCP Gateway"
    },
    "context-awesome": {
      "command": "curl",
      "args": [
        "-sf",
        "http://localhost:3000/mcp/context-awesome/sse"
      ],
      "description": "Awesome lists via MCP Gateway"
    },
    "duckduckgo": {
      "command": "curl",
      "args": [
        "-sf",
        "http://localhost:3000/mcp/duckduckgo/sse"
      ],
      "description": "Web search via MCP Gateway"
    },
    "brain-rules": {
      "command": "python3",
      "args": [
        "${BRAIN_DIR}/mcp/brain-mcp-server/server.py"
      ],
      "description": "Brain repo rules and agents (local)"
    }
  }
}
