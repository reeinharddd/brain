#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
cd "$ROOT"

bash ./utils/scripts/validate-markdown-boundary.sh
bash ./utils/scripts/validate-docs-incremental.sh --staged
