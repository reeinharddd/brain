# Canonical Brain Control Plane Manifest
# This is the root manifest that orchestrates the configuration sync across IDE targets.
# It does NOT replace registry.yml or providers.yml, it references them.

version: "1.0"

settings:
  backup_enabled: false
  backup_dir: ~/.brain/backups
  backup_retention_days: 7
  dry_run_by_default: false

domains:
  skills:
    source: "artifacts/skills/registry.yml"
    enabled: true
  mcp:
    source: "artifacts/mcps/registry.yml"
    enabled: true
  providers:
    source: "artifacts/providers/providers.yml"
    enabled: true
  rules:
    source: "artifacts/rules/canonical.md"
    enabled: true
  agents:
    source: "artifacts/agents/"
    enabled: true

targets:
  vscode:
    enabled: true
    type: "copilot"
    output_dirs:
      instructions: "~/.config/Code/User/prompts/"
      mcp: "~/.config/Code/User/globalStorage/rooveterinaryinc.roo-cline/settings/cline_mcp_settings.json"
    managed_sections: true

  cursor:
    enabled: true
    type: "cursor"
    output_dirs:
      rules: "~/.cursorrules"
    managed_sections: true

  windsurf:
    enabled: false
    type: "windsurf"
    output_dirs:
      rules: "~/.windsurfrules"
      mcp: "~/.codeium/windsurf/mcp.json"
    managed_sections: true

  claude_code:
    enabled: true
    type: "claude-code"
    output_dirs:
      mcp: "~/.claude.json"
    managed_sections: false

  cli:
    enabled: true
    type: "brain-cli"
    output_dirs:
      skills: "~/.brain/config/skills.json"
      mcp: "~/.brain/config/mcps.json"
      agents: "~/.brain/config/agents.json"
    managed_sections: false
