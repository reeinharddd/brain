apiVersion: brain/v1
kind: command
id: consolidate
displayName: Consolidate
description: Brain command artifact for Consolidate.
scope: workspace
owner: user:brain
visibility: workspace
lifecycle:
  state: active
source:
  type: local
  uri: local://artifacts/commands/consolidate.md
sync:
  mode: hybrid
  cloudEnabled: true
security:
  trust: verified
  permissions:
    fs: read
    net: none
    exec: none
content:
  entrypoint: artifacts/commands/consolidate.md
  files:
    - artifacts/commands/consolidate.md
command:
  trigger: /consolidate
  autoInvokePolicy: suggest
  outputContract:
    format: markdown
