apiVersion: brain/v1
kind: command
id: test
displayName: Test
description: Brain command artifact for Test.
scope: workspace
owner: user:brain
visibility: workspace
lifecycle:
  state: active
source:
  type: local
  uri: local://artifacts/commands/test.md
sync:
  mode: hybrid
  cloudEnabled: true
security:
  trust: verified
  permissions:
    fs: read
    net: none
    exec: none
content:
  entrypoint: artifacts/commands/test.md
  files:
    - artifacts/commands/test.md
command:
  trigger: /test
  autoInvokePolicy: suggest
  outputContract:
    format: markdown
