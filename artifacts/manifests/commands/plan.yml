apiVersion: brain/v1
kind: command
id: plan
displayName: Plan
description: Brain command artifact for Plan.
scope: workspace
owner: user:brain
visibility: workspace
lifecycle:
  state: active
source:
  type: local
  uri: local://artifacts/commands/plan.md
sync:
  mode: hybrid
  cloudEnabled: true
security:
  trust: verified
  permissions:
    fs: read
    net: none
    exec: none
content:
  entrypoint: artifacts/commands/plan.md
  files:
    - artifacts/commands/plan.md
command:
  trigger: /plan
  autoInvokePolicy: suggest
  outputContract:
    format: markdown
