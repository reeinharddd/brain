apiVersion: brain/v1
kind: command
id: standup
displayName: Standup
description: Brain command artifact for Standup.
scope: workspace
owner: user:brain
visibility: workspace
lifecycle:
  state: active
source:
  type: local
  uri: local://artifacts/commands/standup.md
sync:
  mode: hybrid
  cloudEnabled: true
security:
  trust: verified
  permissions:
    fs: read
    net: none
    exec: none
content:
  entrypoint: artifacts/commands/standup.md
  files:
    - artifacts/commands/standup.md
command:
  trigger: /standup
  autoInvokePolicy: suggest
  outputContract:
    format: markdown
